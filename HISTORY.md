# HISTORY

sungd.uk 변천사. 최신이 맨 위.

## 2026-08-09 — 이 repo 를 랜딩으로 전환

원래 이력서 전용 repo 였다. 사이트 재편으로 이력서 본문은 `resume` repo(resume.sungd.uk)로
옮기고, 이 repo 는 `sungd.uk` 을 맡아 링크만 있는 한 장짜리 랜딩이 됐다.

**왜**

- 루트 주소를 에세이 블로그가 쓰고 있어 나머지 사이트가 형제인지 자식인지 애매했다
- 링크트리처럼 링크만 나열하는 대신, 소개 한 줄을 얹어 그 자체로 목적지가 되게 했다
- 이력서는 프로젝트와 함께 보는 게 맞아서 그쪽으로 합쳤다

**한 것**

- `index.html` 을 소개 + 세 사이트 링크로 다시 씀 (스타일 인라인, 빌드 없음)
- 이력서 자산(`en/`·`style.css`·`script.js`·`fonts/`)은 옮겨간 쪽으로. 이 repo 에서는 제거
- 2026-08-09 이전 기록(이력서 페이지 변천사)은 `resume` repo 의 `HISTORY.md` 로 함께 옮겼다

## 2026-08-10 — 옛 글 주소 안내와 방문 집계

`sungd.uk` 은 원래 글 블로그였다. 예전 링크·북마크가 전부 이 사이트로 오는데 GitHub 기본
404 가 떠서 갈 곳을 알 수 없었다.

- `404.html` 추가 — 들어온 주소를 그대로 `writing.sungd.uk` 에서 여는 링크를 붙인다
- 방문 집계 추가 — 404 가 얼마나 열리는지 보고 넘김(리다이렉트)을 걸지 정하려는 것

넘김을 안 걸기로 한 대신 둔 장치다. 조회수가 꾸준하면 Cloudflare 규칙을 넣는다
(조건식은 `resume` repo 의 `docs/reorg/cloudflare.md`).
