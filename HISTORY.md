## 2026-07-25 — 정리 패스 + 영어 버전

인라인 스타일로 덧대다 15회 넘게 부분수정되어 구조가 지저분해진 시점에 전체 새로쓰기.
같은 골격으로 영어 페이지(`/en/`)를 추가.

**왜**
- 모든 스타일이 인라인이라 모바일 대응에 `!important`를 계속 붙여야 했다. 클래스로 빼면서 전부 제거.
- 영어 버전을 만들려면 어차피 스타일 공유가 필요했다 — 정리와 동시에 하는 게 맞았음.

**결정**
- `style.css` 하나를 두 언어가 공유. 디자인 수정은 여기 한 곳에서.
- `script.js`가 `<html lang>`을 보고 재직 기간 표기를 고름("4년 5개월" / "4 yrs 5 mos").
  HTML에는 `data-from`/`data-to`만 두어 기간 문자열이 코드에 하드코딩되지 않게.
- 연도+내용 목록(수상·논문)을 grid로 바꿔 `margin-left: 3.5rem` 같은 매직넘버 제거.
- `sup`을 `em` 상대 크기 한 규칙으로 통일(0.5~0.65rem이 제각각이었음).
- 섹션 제목 아래 여백을 0.75rem으로 통일(Experience만 1.25rem이었음).
- 보이는 디자인은 유지 — 사용자가 tech.sungd.uk 포맷을 유지하길 원했으므로 토큰·타이포·다크모드 그대로.

## 2026-07-25 — 신설: tech.sungd.uk/resume/ 대체

tech.sungd.uk 안에 손으로 쓴 정적 이력서 페이지가 있었으나 내용이 자주 밀렸다(마스터 파이프라인과 별개로 관리됨).
`dev/apps/resume`의 master 빌드 파이프라인 산출물을 그대로 옮겨와 독립 도메인으로 분리.

**왜**
- 이력서는 독립 도메인이 더 적절 — 명함·이메일 서명·지원서에 붙이기 깔끔한 URL.
- 손으로 쓴 페이지는 master 갱신과 따로 놀아 내용이 stale해지는 문제가 있었음. 파이프라인 산출물을 그대로 배포하면 갱신 경로가 하나로 합쳐진다.

**결정**
- 도메인: `resume.sungd.uk` (검토: `cv.sungd.uk`도 후보였으나 더 보편적인 표현을 택함)
- 배포: GitHub Pages, 빌드 스텝 없이 정적 파일 그대로(`ai-pick`과 같은 legacy 방식)
- `dev/apps/resume/build/build.mjs`에 `--public`(커리어 키트 전용 `/d/` 토글 숨김)·`--dlbar`(PDF 다운로드 바) 옵션 추가해 독립 배포용 렌더를 지원
- 내용 범위: 상세 경력기술서·포트폴리오는 빼고 **1페이지 요약**만 — 사용자 승인. tech.sungd.uk/resume/의 원래 분량(소개·경력·스킬·학력·논문/대회/외국어)에 맞춤

## 2026-07-25 — 포맷·내용을 옛 페이지 그대로로 되돌림

master 파이프라인 디자인(`build/design.css`)으로 먼저 배포했다가, 사용자가 되돌리라고 함 —
**포맷도 내용도 tech.sungd.uk/resume/ 그대로**로 시작하고 싶다는 요청. 새 파이프라인 디자인·내용은
사용자 승인 없이 올라간 것이라 롤백.

**왜**
- 사용자가 이력서 내용은 반드시 본인 확인 받고 올리라고 명시. master 데이터를 자동으로 다 반영하는 건 그 확인 없이 진행한 것이었음.
- 포맷(디자인)도 나중에 따로 바꾸고 싶어함 — 지금은 원래 포맷 유지가 우선.

**결정**
- `index.html`을 tech.sungd.uk/resume/의 원본 마크업·스타일로 재구성(Astro 의존 제거, 순수 정적 HTML). 원본 폰트(Inter, 자체 호스팅)·파비콘도 그대로 이식.
- 상단에 "PDF 다운로드" 링크만 작게 추가(요구사항이라 유지), 나머지 포맷·내용은 원본과 동일.
- 다음 단계: `dev/apps/resume` master 데이터에서 무엇을 이 포맷에 반영할지 사용자와 항목별로 정한 뒤 갱신. 디자인 변경도 그다음 별도 논의.
